/**
 * [ ] Use console.info para dizer "Sistema carregando...".
 */
const prompt = require("prompt-sync")({ sigint: true });

console.info("Sistem carregando");
/**
 * console.info é usado para mensagens informativas.
 *  No terminal do VS Code ele aparece igual ao log,
 *  mas em navegadores ou logs de servidores (como o Node.js em produção),
 *  ele ajuda a organizar o que é apenas informação e o que é erro ou aviso.
 * Dica: É exatamente a mesma sintaxe do console.log, mas com info.
 *
 */
