/**
 * [ ] Atribua o valor de uma variável a outra.
 */
const prompt = require("prompt-sync")({ sigint: true });

let rafael = 10;// criamos a primeira variavel

let pinguim;// criamos a segunda vazia 

pinguim = rafael// fizemos atribuicao o valor rafael 10 entra na pinguim

console.log(pinguim);
